<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jcodee.mod1class1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Declaramos un activity
         portrait = vertical
         landscape = horizontal -->
        <activity
            android:name=".HomeActivity"
            android:screenOrientation="portrait">
            <!-- Agregamos la referencia para que se pueda lanzar automaticamente
             y como primera pantalla -->
            <intent-filter>
                <!-- Indicamos que la pantalla va a ser principal -->
                <action android:name="android.intent.action.MAIN" />

                <!-- Indicamos que la pantalla va a lanzarse automaticamente -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>